/*
	//**********************************************
	//Initialize the client
	//**********************************************
	if(FAILED(Client.Initialize(this)))
		return E_FAIL;

	if(FAILED(Client.InitSocket()))
		return E_FAIL;*/