#ifndef ENTITY_H
#define ENTITY_H

#include <vector>

#include "game/Component.h"
/*
static g_id;

class Entity
{
public:
	Entity(EntityManager* pManager)
	{
		g_id++;
		m_entityID = g_id;
	}
	//Add and remove components
	UINT addComponent(Component* m_pComponent);
	void removeComponent(UINT ID);
private:
	std::vector<Component*> m_componentList;
	UINT                    m_entityID;
};*/

#endif