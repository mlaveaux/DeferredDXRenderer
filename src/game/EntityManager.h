#ifndef ENTITYMANAGER_H
#define ENTITYMANAGER_H

#include <vector>
#include "Entity.h"
/*
class EntityManager
{
public:
	EntityManager()
	{
		m_entityArray.reserve(256);
	}
	Entity* createEntity()
	{
		m_entityArray.push_back(new Entity(this));

		return m_entityArray[m_entityArray.back()];
	}
	void removeEntity()
	{

	}
private:
	std::vector<Entity*> m_entityArray;
};*/

#endif